#include <bits/stdc++.h>

using namespace std;

void read_input(vector<string>& input) {
    string line;
    while (getline(cin, line)) {
        input.push_back(line);
    }
}

int main() {

    return 0;
}
